<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.HomeNewGoodTitleActivity"></activity>
        <activity android:name=".homework.HomeWork2Activity" />
        <activity android:name=".homework.HomeWorkActivity" />
        <activity android:name=".ui.home.HomeBrandTitleActivity" />
        <activity android:name=".ui.home.BrandInfoActivity" />
        <activity android:name=".mytongpao.TongPaoActivity" />
        <activity android:name=".ui.SpalceActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvvm.NewBindActivity"
            android:label="@string/title_activity_new_bind" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>